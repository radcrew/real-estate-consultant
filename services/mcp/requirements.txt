# Vercel Python runtime installs from this file (keep in sync with pyproject.toml).
httpx>=0.27.0
# Stay on SDK v1: v2 drops mcp.server.fastmcp (renamed MCPServer).
mcp[cli]>=1.9.0,<2
pydantic-settings>=2.6.0
