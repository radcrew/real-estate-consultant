# Editable install from pyproject.toml (preferred):
#   python -m pip install -e ".[dev]"
#
# Pinned loosely for environments that prefer requirements.txt:
httpx>=0.27.0
mcp[cli]>=1.9.0
pydantic-settings>=2.6.0
